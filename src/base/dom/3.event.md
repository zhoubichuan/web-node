---
lang: zh-CN
sidebarDepth: 2
meta:
  - name: description
    content: 个人总结的vuepress学习技术文档-语法
  - name: keywords
    content: vuepress,最新技术文档,vuepress语法,markdown语法
---

# 文件

## 1.获取目录下列表

::: demo

```vue
<template>
  <div class="fs-box">
     <input type="text" v-model="path" style="width: calc(100% - 100px);" />
      <input type="text" v-model="foldName" style="width: calc(100% - 100px);"  />
     <input type="text" v-model="count" style="width: calc(100% - 100px);"  />
    <button @click="handleList">获取根目录</button>
    根目录：{{fold}}
  </div>
</template>
<script>
export default {
  data() {
    return {
      fold: '',
      path: '',
      foldName:'my-image',
      count:6
    }
  },
  mounted() {
    // this.handleList()
   this.path = localstorage.getItem('path')
  },
  methods: {
    async handleList(){
      const res = await this.$api.file({ type: 'move', path: this.path ,foldName:[this.foldName],count:this.count})
      if (!res) return
      this.fold = res
    }
  }
}
</script>
```

:::
